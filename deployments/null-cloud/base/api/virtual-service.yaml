apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: eddington-ui-vs 
spec:
  hosts:
  - "*"
  gateways:
  - nullcloud-gateway
  http:
  - match:
    - uri:
        prefix: /api
  - route:
    - destination:
        host: eddington-api
        port:
          number: 80
