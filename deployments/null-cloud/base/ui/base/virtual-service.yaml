apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: eddington-ui-vitrual-service 
spec:
  hosts:
  - "*"
  gateways:
  - nullcloud-gateway
  http:
  - route:
    - destination:
        host: eddington-ui
        port:
          number: 80

